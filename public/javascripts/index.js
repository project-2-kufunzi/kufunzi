window.onload = () => {
  const footer = document.querySelector('footer')
  footer.classList = 'hide'

  /*   new Vivus('kufunzi-logo', {
      start: 'autostart',
      type: 'delayed',
      duration: 300,
      animTimingFunction: Vivus.EASEs
    }, logo => {
      setTimeout(() => {
        logo.reset().play();
      }, 3000);
    });
   */
}